# سند فنی محصول پتار | نسخه توسعه دهندگان

> مخاطب: مدیر فنی، توسعه دهنده فرانت اند، توسعه دهنده بکند و طراح سیستم.
> وضعیت: نسخه تولید.
> معماری: فرانت اند Next.js و بکند Django با Django REST Framework.

---

## 0. 🧭 راهنمای مطالعه این سند

| نقش خواننده | بخش های پیشنهادی |
| --- | --- |
| مدیر فنی | بخش 1 تا 3، بخش 6 تا 9، بخش 14 و 15 |
| توسعه دهنده فرانت اند | بخش 3 تا 5، بخش 9 تا 11 |
| توسعه دهنده بکند | بخش 3 تا 10، بخش 12 تا 14 |
| توسعه دهنده تازه کار | کل سند به ترتیب، چون مفاهیم پایه هم توضیح داده شده است |

قرارداد نام گذاری در این سند: نام فیلدها و مسیرهای API به انگلیسی نوشته می شود چون عینا در کد استفاده می شود. بقیه متن کاملا فارسی است.

---

## 1. 🏗 نمای کلی سیستم

### اجزا به زبان ساده

1. فرانت اند Next.js همان چیزی است که کاربر در مرورگر می بیند و با آن کار می کند. صفحه ها را نمایش می دهد و با بکند حرف می زند.
2. بکند Django همان مغز سیستم است. قوانین را اجرا می کند، امتیاز را حساب می کند، دسترسی را کنترل می کند و داده را در دیتابیس نگه می دارد.
3. API با Django REST Framework زبان مشترک فرانت و بکند است. فرانت درخواست می فرستد و بکند پاسخ ساختار یافته بر می گرداند.
4. دیتابیس PostgreSQL محل اصلی ذخیره همه داده هاست.
5. Redis حافظه سریع کمکی برای کش، کدهای تایید و محدودیت نرخ است.
6. Celery مجری کارهای پس زمینه مثل ساخت رتبه بندی و ارسال پیام است.
7. فضای ذخیره فایل محل نگه داری تصویر، صوت و فایل های آپلود شده است.

### مسیر یک درخواست

```text
مرورگر کاربر
↓ HTTPS
فرانت اند Next.js
↓ JSON روی API نسخه بندی شده
بکند Django و DRF
├── بررسی احراز هویت و دسترسی
├── خواندن کش Redis در صورت امکان
├── خواندن یا نوشتن دیتابیس
└── برگرداندن پاسخ JSON
```

### محیط ها

| محیط | کاربرد |
| --- | --- |
| توسعه | کامپیوتر هر توسعه دهنده با داده نمونه |
| staging | تست نهایی قبل از انتشار با داده شبیه تولید |
| تولید | محیط واقعی کاربران |

---

## 2. 👥 کاربران و نقش ها

### سه نقش سیستم

| نقش | مقدار فیلد role | چه کسی است | چه کار می کند |
| --- | --- | --- | --- |
| دانش آموز | student | کاربر نهایی محصول | ثبت نام، یادگیری، امتیاز و رتبه بندی |
| تیم محتوا | staff | تولید کننده و ناظر داخلی | ساخت، ویرایش و بازبینی محتوا |
| ادمین ارشد | admin | مدیر پلتفرم | تایید نهایی، انتشار، مدیریت کاربران و گزارش ها |

نکته مهم: مدرسه هیچ نقشی در سیستم ندارد و هیچ حسابی برای مدرسه ساخته نمی شود. مدرسه فقط شریک معرفی و بازاریابی است و مشخصات آن به صورت یک رکورد اطلاعاتی توسط ادمین ثبت می شود.

### تفاوت تولید کننده و ناظر داخل تیم محتوا

هر دو role برابر staff دارند ولی با پرچم دسترسی از هم جدا می شوند:

| پرچم | معنی | چه کسی دارد |
| --- | --- | --- |
| can_create | اجازه ساخت و ویرایش پیش نویس | همه تیم محتوا |
| can_review | اجازه تایید مرحله بازبینی یا برگشت با نظر | فقط ناظرها |
| can_publish | اجازه انتشار نهایی | فقط ادمین ارشد |

### ماتریس دسترسی کامل

| قابلیت | دانش آموز | تیم محتوا | ادمین ارشد |
| --- | --- | --- | --- |
| مشاهده محتوای منتشر شده | دارد | دارد | دارد |
| انجام تجربه و فعالیت و آزمون | دارد | دارد | دارد |
| کسب امتیاز و نشان | دارد | ندارد | ندارد |
| حضور در رتبه بندی | دارد | ندارد | ندارد |
| ساخت و ویرایش پیش نویس | ندارد | دارد | دارد |
| بازبینی و برگشت با نظر | ندارد | فقط ناظر | دارد |
| انتشار نهایی و آرشیو | ندارد | ندارد | دارد |
| ساخت کاربر داخلی | ندارد | ندارد | دارد |
| مشاهده و مسدود سازی دانش آموز | ندارد | ندارد | دارد |
| ثبت رکورد مدرسه همکار | ندارد | ندارد | دارد |
| مشاهده گزارش ها | ندارد | محدود به محتوای خود | کامل |
| مشاهده لاگ تغییرات | ندارد | دارد | دارد |

قانون امنیتی پایه که همه باید بدانند: کنترل دسترسی فقط مخفی کردن دکمه در فرانت نیست. هر API در بکند باید خودش نقش کاربر را بررسی کند. فرانت برای تجربه کاربری دکمه را مخفی می کند ولی امنیت واقعی در بکند است.

---

## 3. 🔐 حساب و احراز هویت

### روش های ورود

چون تصمیم نهایی درباره روش ورود گرفته نشده است، هر سه روش پشتیبانی می شود:

| روش | شناسه ورود | رمز | کد یک بار مصرف |
| --- | --- | --- | --- |
| موبایل | شماره موبایل | اختیاری | دارد، با پیامک |
| ایمیل | ایمیل | دارد | دارد، با ایمیل |
| نام کاربری | نام کاربری | دارد | ندارد |

منظور از کد یک بار مصرف یا OTP یک عدد چند رقمی است که برای کاربر ارسال می شود و چند دقیقه اعتبار دارد. کاربر به جای رمز، آن کد را وارد می کند.

### تعریف های پایه برای تازه کارها

1. ثبت نام یعنی ساخت حساب جدید.
2. ورود یعنی اثبات اینکه صاحب حساب هستید.
3. توکن دسترسی یا Access Token یک رشته متنی است که همراه هر درخواست فرستاده می شود تا بکند بفهمد شما کی هستید. عمر آن کوتاه است.
4. توکن تازه سازی یا Refresh Token عمر طولانی تری دارد و فقط برای گرفتن توکن دسترسی جدید استفاده می شود.
5. تایید دو مرحله ای فعلا نداریم ولی طراحی باید اجازه اضافه شدن آن را در آینده بدهد.

### جریان ثبت نام با موبایل

```text
وارد کردن شماره موبایل
↓
ارسال کد تایید با محدودیت نرخ
↓
وارد کردن کد در فرم
↓
ساخت حساب و صدور توکن ها
↓
تکمیل نام نمایشی
↓
ورود به خانه
```

### جریان ثبت نام با ایمیل یا نام کاربری

```text
وارد کردن ایمیل یا نام کاربری به همراه رمز عبور
↓
بررسی قوانین رمز و یکتا بودن شناسه
↓
ساخت حساب
↓
ارسال کد فعال سازی برای ایمیل
↓
تکمیل نام نمایشی
↓
ورود به خانه
```

### جریان ورود با رمز عبور

```text
وارد کردن شناسه و رمز
↓
بررسی رمز و وضعیت حساب
↓
صدور توکن ها در صورت درست بودن
↓
ثبت زمان آخرین ورود
```

### جریان ورود با کد یک بار مصرف

```text
وارد کردن موبایل یا ایمیل
↓
ارسال کد با محدودیت نرخ
↓
وارد کردن کد
↓
صدور توکن ها در صورت درست بودن کد
```

### جریان فراموشی رمز

```text
درخواست بازیابی با ایمیل یا موبایل
↓
ارسال کد تایید
↓
وارد کردن کد و رمز جدید
↓
ابطال توکن های قبلی
↓
ورود با رمز جدید
```

### قوانین امنیتی احراز هویت

| کد قانون | قانون | دلیل |
| --- | --- | --- |
| BR-AUTH-1 | رمز عبور فقط به صورت هش ذخیره می شود و هیچ وقت متن ساده آن نگه داری نمی شود | لو رفتن دیتابیس نباید رمزها را لو بدهد |
| BR-AUTH-2 | کد تایید حداکثر پنج دقیقه اعتبار دارد | پنجره سوء استفاده کوتاه شود |
| BR-AUTH-3 | هر کد حداکثر پنج بار قابل امتحان است | جلوگیری از حدس زدن کد |
| BR-AUTH-4 | ارسال مجدد کد حداقل شصت ثانیه فاصله دارد | جلوگیری از اسپم و هزینه اضافه |
| BR-AUTH-5 | بعد از پنج ورود ناموفق، حساب پانزده دقیقه قفل می شود | جلوگیری از حمله حدس رمز |
| BR-AUTH-6 | عمر توکن دسترسی کوتاه و عمر توکن تازه سازی محدود است | لو رفتن توکن کم خطر شود |
| BR-AUTH-7 | با تغییر رمز، همه توکن های قبلی باطل می شود | دستگاه گم شده یا مشکوک از دسترس خارج شود |
| BR-AUTH-8 | حساب داخلی فقط توسط ادمین ارشد ساخته می شود | جلوگیری از ارتقای غیر مجاز دسترسی |
| BR-AUTH-9 | همه تلاش های ناموفق ورود با آی پی و زمان ثبت می شود | امکان بررسی حمله و مسدود سازی |

### API های احراز هویت

| روش | مسیر | هدف | دسترسی |
| --- | --- | --- | --- |
| POST | /api/v1/auth/register | ثبت نام با موبایل یا ایمیل یا نام کاربری | عمومی |
| POST | /api/v1/auth/code/request | درخواست ارسال کد تایید | عمومی با محدودیت نرخ |
| POST | /api/v1/auth/code/verify | تایید کد و صدور توکن | عمومی با محدودیت نرخ |
| POST | /api/v1/auth/login | ورود با شناسه و رمز | عمومی با محدودیت نرخ |
| POST | /api/v1/auth/token/refresh | گرفتن توکن دسترسی جدید | دارنده توکن تازه سازی |
| POST | /api/v1/auth/logout | خروج و ابطال توکن تازه سازی | وارد شده |
| POST | /api/v1/auth/password/change | تغییر رمز با دانستن رمز قبلی | وارد شده |
| POST | /api/v1/auth/password/reset/request | درخواست کد بازیابی رمز | عمومی با محدودیت نرخ |
| POST | /api/v1/auth/password/reset/confirm | تنظیم رمز جدید با کد | عمومی با محدودیت نرخ |
| GET | /api/v1/auth/me | دریافت اطلاعات حساب خودم | وارد شده |
| PATCH | /api/v1/auth/me | ویرایش نام نمایشی و تصویر | وارد شده |

### وظایف فرانت در احراز هویت

1. فرم های ثبت نام، ورود، کد تایید و بازیابی رمز.
2. نگه داری امن توکن ها و ارسال خودکار آن در هر درخواست.
3. تازه سازی خودکار توکن دسترسی با توکن تازه سازی.
4. هدایت کاربر وارد نشده به صفحه ورود هنگام باز کردن صفحه خصوصی.
5. نمایش خطاهای فارسی بکند بدون تغییر معنا.

توصیه امنیتی فرانت: توکن تازه سازی در کوکی امن و غیر قابل خواندن توسط جاوا اسکریپت نگه داری شود و توکن دسترسی در حافظه نگه داشته شود تا ریسک سرقت کمتر شود.

### وظایف بکند در احراز هویت

1. اعتبارسنجی شناسه، رمز و کد.
2. هش کردن رمز و مقایسه امن.
3. تولید، ارسال، انقضا و ابطال کدهای تایید.
4. صدور و ابطال توکن ها.
5. اعمال قفل موقت و محدودیت نرخ.
6. ثبت لاگ تلاش های مشکوک.

---

## 4. 📄 مشخصات صفحه به صفحه

برای هر صفحه پنج چیز مشخص شده است: داستان کاربر یعنی چه کسی چه می خواهد، سفر کاربر یعنی قدم های او، چه می بیند یعنی بخش های صفحه، با چه چیزی تعامل می کند یعنی کلیک ها و فرم ها، و چه API هایی صدا زده می شود.

### 4.1 🏠 صفحه خانه | مسیر /

داستان کاربر: به عنوان بازدید کننده جدید می خواهم در یک نگاه بفهمم پتار چیست و یک تجربه را امتحان کنم.

سفر کاربر:

1. وارد خانه می شود.
2. تیتر و توضیح کوتاه را می خواند.
3. نمودار از آیه تا زندگی را می بیند.
4. روی یک تجربه پیشنهادی کلیک می کند.
5. در صورت تمایل فرم تماس مدرسه را پر می کند.

چه می بیند: سربرگ، معرفی اصلی، نمودار نه گام، تجربه های منتخب، موضوعات، مسیرهای پیشنهادی، بنر کمپین جاری، مزایا، سوالات پر تکرار، فرم تماس مدرسه و پانوشت.

تعامل ها: کلیک روی کارت ها، باز کردن سوالات، ثبت فرم تماس.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/home | دریافت همه بلوک های خانه در یک پاسخ |
| GET | /api/v1/experiences/featured | تجربه های منتخب تحریریه |
| GET | /api/v1/paths/featured | مسیرهای پیشنهادی |
| GET | /api/v1/campaigns/active | کمپین جاری بر اساس تاریخ امروز |
| POST | /api/v1/leads | ثبت فرم تماس مدرسه |

عملیات داده: فقط خواندن به جز ثبت فرم تماس. محتوای صفحه از کش Redis خوانده می شود و با هر انتشار باطل می گردد.

قوانین:

| کد | قانون |
| --- | --- |
| BR-HOME-1 | بنر کمپین فقط وقتی نمایش داده می شود که امروز بین تاریخ شروع و پایان باشد |
| BR-HOME-2 | فرم تماس مدرسه هیچ حسابی نمی سازد و فقط یک رکورد درخواست ثبت می کند |
| BR-HOME-3 | تجربه های منتخب را تحریریه انتخاب می کند و ترتیب آن دستی است |

### 4.2 🔍 کشف و جستجو | مسیر /discover

داستان کاربر: به عنوان دانش آموز می خواهم با چند فیلتر ساده به محتوای مناسب خودم برسم.

سفر کاربر:

1. وارد کشف می شود.
2. کلمه ای تایپ می کند یا فیلتر موضوع را می زند.
3. نتایج را می بیند و مرتب می کند.
4. روی یک نتیجه کلیک می کند.

چه می بیند: نوار جستجو، فیلترهای موضوع و مفهوم و سوره و سطح و نوع و زمان، فهرست نتایج و حالت بدون نتیجه با پیشنهاد جایگزین.

تعامل ها: تایپ، انتخاب فیلتر، حذف فیلتر، مرتب سازی و کلیک روی نتیجه.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/search?q=&topic=&level=&type=&surah=&ordering= | جستجو و فیلتر ترکیبی |
| GET | /api/v1/topics | فهرست موضوعات برای فیلتر |
| GET | /api/v1/concepts | فهرست مفاهیم برای فیلتر |

عملیات داده: فقط خواندن. جستجوی متنی در فاز اول با جستجوی دیتابیس انجام می شود و موتور جستجوی جدا لازم نیست.

قوانین:

| کد | قانون |
| --- | --- |
| BR-SEARCH-1 | فقط محتوای منتشر شده در نتایج بر می گردد |
| BR-SEARCH-2 | ترکیب چند فیلتر با منطق و انجام می شود |
| BR-SEARCH-3 | جستجوی خالی منتخب های تحریریه را بر می گرداند |

### 4.3 ✨ تجربه یادگیری | مسیر /experiences/[id]

داستان کاربر: به عنوان دانش آموز می خواهم یک موضوع را قدم به قدم تجربه کنم و در پایان بدانم در زندگی چه کار کنم.

سفر کاربر:

1. وارد صفحه می شود و هدف و زمان را می بیند.
2. آیه و ترجمه و مفهوم را می خواند.
3. مثال و سناریو را دنبال می کند.
4. به سوال فکر می کند.
5. فعالیت را انجام می دهد و بازخورد می گیرد.
6. تامل شخصی را می نویسد.
7. امتیاز و پیشنهاد ادامه را می بیند.

چه می بیند: سربرگ تجربه، نوار پیشرفت، بلوک آیه با صوت، ترجمه، کارت مفهوم، مثال، سناریو، سوال، فعالیت، کادر تامل و بلوک پایان با امتیاز و ادامه مسیر.

تعامل ها: پخش صوت، پاسخ به فعالیت، نوشتن و ثبت تامل، ذخیره، رفتن به تجربه بعدی.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/experiences/:id | دریافت کامل تجربه با نه گام |
| POST | /api/v1/experiences/:id/start | ثبت شروع تجربه |
| POST | /api/v1/activities/:id/submit | ثبت پاسخ فعالیت و گرفتن نتیجه و امتیاز |
| POST | /api/v1/experiences/:id/reflect | ثبت متن تامل شخصی |
| POST | /api/v1/experiences/:id/complete | ثبت تکمیل نهایی و جمع امتیاز |

عملیات داده: خواندن محتوای تجربه از کش، نوشتن شروع و پاسخ و تامل در دیتابیس. نمره دهی فقط در سرور انجام می شود و امتیاز ارسالی از فرانت قبول نیست.

قوانین:

| کد | قانون |
| --- | --- |
| BR-EXP-1 | تجربه بدون فعالیت قابل انتشار نیست |
| BR-EXP-2 | تکمیل فقط وقتی ثبت می شود که فعالیت و تامل هر دو انجام شده باشند |
| BR-EXP-3 | تامل شخصی خصوصی است و در هیچ API عمومی بر نمی گردد |
| BR-EXP-4 | ثبت شروع تکراری رکورد جدید نمی سازد و همان رکورد باز بر می گردد |

### 4.4 📖 بخش قرآن | مسیرهای /quran

داستان کاربر: به عنوان دانش آموز می خواهم آیه اصلی یک تجربه را ببینم و از آیه به تجربه های مرتبط بروم.

سفر کاربر:

1. فهرست سوره ها را می بیند.
2. وارد یک سوره می شود و آیه را پیدا می کند.
3. متن، ترجمه و مفهوم را می خواند.
4. روی تجربه مرتبط کلیک می کند.

چه می بیند: فهرست سوره ها، صفحه سوره با آیه ها، صفحه آیه با متن و ترجمه و مفهوم و تجربه ها و دکمه قبلی و بعدی.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/surahs | فهرست سوره ها |
| GET | /api/v1/surahs/:id | مشخصات سوره با فهرست آیه ها |
| GET | /api/v1/verses/:id | جزئیات آیه با مفهوم و تجربه های مرتبط |

عملیات داده: فقط خواندن و کاملا قابل کش چون متن آیات تقریبا ثابت است.

قوانین:

| کد | قانون |
| --- | --- |
| BR-QURAN-1 | متن و ترجمه از منبع ثابت می آید و از API ویرایش پذیر نیست |
| BR-QURAN-2 | آیه بدون تجربه مرتبط بخش تجربه را نمایش نمی دهد |
| BR-QURAN-3 | ناوبری قبلی و بعدی در مرز سوره به درستی مدیریت می شود |

### 4.5 🛤 مسیر یادگیری | مسیر /paths/[id]

داستان کاربر: به عنوان دانش آموز می خواهم یک برنامه چند مرحله ای داشته باشم و آخرش نشان بگیرم.

سفر کاربر:

1. هدف و مراحل و زمان کل را می بیند.
2. تجربه اول را شروع می کند.
3. هر روز یک قدم جلو می رود و درصد پیشرفت را می بیند.
4. آزمون پایانی را می دهد و نشان می گیرد.

چه می بیند: سربرگ مسیر، نوار پیشرفت شخصی، فهرست مراحل به ترتیب با وضعیت هر کدام، آزمون پایانی و پیام اتمام با نشان.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/paths/:id | جزئیات مسیر با مراحل و پیشرفت من |
| POST | /api/v1/paths/:id/start | شروع مسیر |
| GET | /api/v1/quizzes/:id | دریافت آزمون پایانی |
| POST | /api/v1/quizzes/:id/submit | ثبت پاسخ آزمون و گرفتن نتیجه |

قوانین:

| کد | قانون |
| --- | --- |
| BR-PATH-1 | ترتیب مراحل همان ترتیب ذخیره شده است و فرانت حق مرتب سازی مجدد ندارد |
| BR-PATH-2 | نشان مسیر فقط با تکمیل همه مراحل و قبولی آزمون پایانی داده می شود |
| BR-PATH-3 | درصد پیشرفت فقط از مراحل واقعا تکمیل شده حساب می شود |

### 4.6 📣 کمپین | مسیر /campaigns/[id]

داستان کاربر: به عنوان دانش آموز می خواهم در برنامه ویژه ماه رمضان شرکت کنم و رتبه ام را ببینم.

سفر کاربر:

1. بنر کمپین را در خانه می بیند.
2. وارد صفحه کمپین می شود و برنامه را می بیند.
3. تجربه های کمپین را انجام می دهد.
4. رتبه خود را در جدول ویژه کمپین می بیند.

چه می بیند: بنر، پیام کمپین، شمارش معکوس پایان، فهرست تجربه ها و مسیرها، جدول رتبه بندی ویژه و دکمه شروع.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/campaigns/active | کمپین جاری |
| GET | /api/v1/campaigns/:id | جزئیات کمپین با محتواها |
| GET | /api/v1/campaigns/:id/leaderboard | رتبه بندی ویژه کمپین |

قوانین:

| کد | قانون |
| --- | --- |
| BR-CAMP-1 | محتوای کمپین ارجاع به محتوای موجود است و کپی جدا ساخته نمی شود |
| BR-CAMP-2 | بعد از پایان تاریخ، بنر غیر فعال و صفحه به حالت آرشیو می رود |
| BR-CAMP-3 | امتیاز کمپین در همان کیف امتیاز کلی حساب می شود و سیستم جدا ندارد |

### 4.7 📈 یادگیری من | مسیر /my-learning

داستان کاربر: به عنوان دانش آموز می خواهم ببینم کجا هستم و با یک کلیک ادامه دهم.

چه می بیند: کارت ادامه بده، آمار شخصی، مسیرهای در حال انجام با درصد، تکمیل شده ها و پیشنهاد بعدی.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/me/progress | خلاصه پیشرفت و آمار |
| GET | /api/v1/me/continue | آخرین نقطه ناتمام برای ادامه |
| GET | /api/v1/me/suggestions | پیشنهاد بعدی |

عملیات داده: فقط خواندن ولی هرگز کش نمی شود چون شخصی است.

### 4.8 👤 پروفایل و نشان ها | مسیر /profile

داستان کاربر: به عنوان دانش آموز می خواهم افتخاراتم را ببینم و بدانم نشان بعدی را چطور بگیرم.

چه می بیند: نام و تصویر، سطح، آمار، دیوار نشان های کسب شده و قفل شده با راهنمای کسب، و تنظیمات حساب.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/me/profile | پروفایل با آمار و سطح |
| GET | /api/v1/me/badges | نشان های من با وضعیت |
| GET | /api/v1/badges | همه نشان های قابل کسب با راهنما |
| PATCH | /api/v1/auth/me | ویرایش پروفایل |
| POST | /api/v1/auth/me/avatar | آپلود تصویر پروفایل |

### 4.9 🏆 رتبه بندی | مسیر /leaderboard

داستان کاربر: به عنوان دانش آموز می خواهم جایگاه خودم را بین دیگران ببینم.

چه می بیند: زبانه هفتگی و کلی، جدول رتبه با نام و امتیاز، جایگاه خودم با هایلایت و توضیح فرمول امتیاز.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/leaderboard/weekly | رتبه بندی هفته جاری |
| GET | /api/v1/leaderboard/all-time | رتبه بندی کلی |
| GET | /api/v1/leaderboard/me | جایگاه من در هر دو جدول |

عملیات داده: جدول ها توسط کار پس زمینه ساخته و در Redis کش می شوند. فرانت هرگز رتبه را حساب نمی کند.

### 4.10 🛠 استودیو محتوا | مسیر /studio

داستان کاربر تیم محتوا: به عنوان تولید کننده می خواهم یک تجربه نه مرحله ای بنویسم و برای بازبینی بفرستم. به عنوان ناظر می خواهم آن را بررسی و تایید یا با نظر برگردانم.

سفر تولید کننده:

1. وارد استودیو می شود و داشبورد وضعیت را می بیند.
2. تجربه جدید می سازد و نه گام را پر می کند.
3. فعالیت و آزمون را اضافه می کند.
4. پیش نمایش زنده را می بیند.
5. برای بازبینی ارسال می کند.

سفر ناظر:

1. صف بازبینی را باز می کند.
2. محتوا را می خواند.
3. تایید می کند یا با نظر متنی بر می گرداند.

چه می بیند: داشبورد وضعیت، فهرست هر نوع محتوا با فیلتر وضعیت، ویرایشگر نه گام، سازنده فعالیت و آزمون و مسیر و کمپین، صف بازبینی و سابقه تغییرات.

| روش | مسیر API | هدف |
| --- | --- | --- |
| GET | /api/v1/studio/dashboard | آمار وضعیت محتواها |
| GET | /api/v1/studio/experiences | فهرست با فیلتر وضعیت |
| POST | /api/v1/studio/experiences | ساخت پیش نویس جدید |
| PUT | /api/v1/studio/experiences/:id | ویرایش کامل پیش نویس |
| POST | /api/v1/studio/experiences/:id/submit | ارسال برای بازبینی |
| POST | /api/v1/studio/experiences/:id/review | تایید ناظر یا برگشت با نظر |
| GET | /api/v1/studio/queue | صف بازبینی ناظر |
| GET | /api/v1/studio/:type/:id/history | سابقه تغییرات یک رکورد |

همین الگو برای فعالیت، آزمون، مسیر و کمپین تکرار می شود.

قوانین:

| کد | قانون |
| --- | --- |
| BR-STUDIO-1 | فقط پیش نویس قابل ویرایش مستقیم است |
| BR-STUDIO-2 | ارسال برای بازبینی فقط از وضعیت پیش نویس ممکن است |
| BR-STUDIO-3 | برگشت ناظر حتما باید نظر متنی داشته باشد |
| BR-STUDIO-4 | ویرایش محتوای منتشر شده آن را به بازبینی بر می گرداند و نسخه عمومی تا تایید جدید سر جایش می ماند |

### 4.11 🛡 مدیریت پلتفرم | مسیر /admin-panel

این بخش با پنل آماده جنگو فرق دارد و یک رابط مدیریتی مخصوص محصول است.

داستان ادمین: به عنوان ادمین ارشد می خواهم محتوای تایید شده را منتشر کنم، کاربران را مدیریت کنم و گزارش بگیرم.

چه می بیند: صف انتشار، مدیریت کاربران داخلی و دانش آموزان، مدیریت کمپین ها و بنرها، ثبت رکورد مدرسه های همکار، گزارش ها و لاگ تغییرات.

| روش | مسیر API | هدف |
| --- | --- | --- |
| POST | /api/v1/manage/:type/:id/publish | انتشار نهایی |
| POST | /api/v1/manage/:type/:id/archive | آرشیو |
| GET | /api/v1/manage/users | جستجوی کاربران با فیلتر نقش |
| POST | /api/v1/manage/users | ساخت کاربر داخلی |
| POST | /api/v1/manage/users/:id/block | مسدود سازی دانش آموز متخلف |
| GET | /api/v1/manage/schools | فهرست مدرسه های همکار |
| POST | /api/v1/manage/schools | ثبت مدرسه همکار جدید |
| GET | /api/v1/manage/reports/overview | گزارش مدیریتی کلی |
| GET | /api/v1/manage/audit-log | لاگ تغییرات مهم |

قوانین:

| کد | قانون |
| --- | --- |
| BR-ADMIN-1 | انتشار فقط از وضعیت تایید شده ممکن است و میانبر وجود ندارد |
| BR-ADMIN-2 | ساخت کاربر داخلی فقط توسط ادمین ارشد ممکن است |
| BR-ADMIN-3 | رکورد مدرسه فقط اطلاعاتی است و هیچ حسابی برای آن ساخته نمی شود |

---

## 5. 🧩 مدل های داده

در این بخش هر جدول معرفی می شود و دلیل تک تک فیلدها توضیح داده می شود. ستون چرا لازم است مهم ترین ستون است و توسعه دهنده تازه کار باید قبل از حذف یا اضافه کردن فیلد آن را بخواند.

### 5.1 👤 مدل User | حساب کاربری

یک رکورد برای هر انسان استفاده کننده سیستم، چه دانش آموز چه تیم محتوا چه ادمین.

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه داخلی کاربر | ارجاع پایدار همه جدول ها به کاربر بدون وابستگی به موبایل یا ایمیل که ممکن است عوض شود |
| username | متن یکتا و اختیاری | نام کاربری برای ورود | پشتیبانی از ورود با نام کاربری برای کسانی که موبایل یا ایمیل ندارند |
| email | ایمیل یکتا و اختیاری | ایمیل کاربر | ورود با ایمیل و ارسال کد و اطلاع رسانی |
| phone | شماره یکتا و اختیاری | شماره موبایل کاربر | ورود با موبایل و ارسال پیامک که روش اصلی دانش آموزان است |
| password_hash | متن هش شده | رمز به صورت غیر قابل بازگشت | امنیت. هیچ وقت متن ساده رمز نگه داری نمی شود |
| display_name | متن | نامی که در محصول نمایش داده می شود | هویت نمایشی جدا از شناسه ورود تا کاربر آزادانه آن را عوض کند |
| avatar | فایل تصویری | تصویر پروفایل | شخصی سازی و حس مالکیت |
| role | انتخابی | نقش کاربر: دانش آموز یا تیم محتوا یا ادمین | پایه همه کنترل های دسترسی در کل سیستم |
| can_review | درست یا غلط | آیا این عضو تیم محتوا ناظر است | جدا کردن حق بازبینی از حق تولید بدون ساخت نقش جدید |
| status | انتخابی | وضعیت حساب: فعال یا مسدود یا حذف شده | مسدود سازی متخلف بدون حذف داده و حفظ تاریخچه |
| failed_login_count | عدد | تعداد ورودهای ناموفق پیاپی | پیاده سازی قفل موقت حساب بعد از حد مجاز |
| locked_until | زمان | پایان قفل موقت | دانستن اینکه حساب تا کی قفل است |
| last_login_at | زمان | آخرین ورود موفق | تشخیص کاربران غیر فعال و تحلیل بازگشت |
| created_at | زمان | زمان ساخت حساب | گزارش رشد و مرتب سازی |
| updated_at | زمان | زمان آخرین تغییر | عیب یابی و همگام سازی |

یکتایی: هر کدام از username و email و phone در صورت پر بودن یکتا هستند. ایندکس روی هر سه برای سرعت ورود لازم است.

### 5.2 🔢 مدل VerificationCode | کد تایید

هر بار که کدی برای کاربر ارسال می شود یک رکورد ساخته می شود.

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه رکورد | رهگیری و لاگ |
| user | ارجاع به کاربر یا خالی | صاحب کد | اتصال کد به حساب در ثبت نام های نیمه تمام ممکن است کاربر هنوز ساخته نشده باشد پس خالی پذیر است |
| target | متن | مقصد ارسال یعنی شماره یا ایمیل | دانستن اینکه کد به کجا فرستاده شده و جلوگیری از سوء استفاده |
| purpose | انتخابی | هدف کد: ثبت نام یا ورود یا بازیابی رمز یا تغییر شماره | جلوگیری از استفاده کد ثبت نام برای بازیابی رمز |
| code_hash | متن هش شده | کد به صورت هش | اگر دیتابیس لو برود کدهای فعال لو نروند |
| attempts | عدد | تعداد تلاش های وارد شده | اعمال سقف حدس زدن |
| expires_at | زمان | انقضای کد | بی اعتبار شدن خودکار بعد از چند دقیقه |
| used_at | زمان یا خالی | زمان مصرف موفق | جلوگیری از استفاده دوباره از یک کد |
| created_at | زمان | زمان ساخت | محاسبه فاصله ارسال مجدد |

دلیل طراحی: کدها در دیتابیس ذخیره می شوند نه فقط Redis تا حسابرسی و عیب یابی ممکن باشد. پاک سازی کدهای منقضی با کار پس زمینه انجام می شود.

### 5.3 ⭐ مدل PointTransaction | کیف امتیاز

به جای نگه داری فقط یک عدد مجموع، هر تغییر امتیاز یک رکورد شفاف دارد. این مهم ترین تصمیم حسابداری امتیاز است.

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه تراکنش | رهگیری و جلوگیری از ثبت دوباره |
| user | ارجاع به کاربر | صاحب امتیاز | اتصال به دانش آموز |
| amount | عدد مثبت یا منفی | مقدار تغییر | ثبت شفاف هر کسب و هر کسر احتمالی |
| reason | انتخابی | دلیل: تکمیل تجربه یا فعالیت یا آزمون یا مسیر یا رکورد یا تامل | گزارش گیری و توضیح به کاربر که این امتیاز از کجا آمده |
| reference_id | متن یا خالی | شناسه رکورد مرتبط مثل شناسه فعالیت | جلوگیری از ثبت دوباره امتیاز یک عمل و امکان ممیزی |
| created_at | زمان | زمان ثبت | محاسبه امتیاز هفتگی برای رتبه بندی |

دلیل طراحی: مجموع امتیاز کاربر همیشه از جمع این جدول حساب می شود یا در فیلد کش شده نگه داری و با این جدول تطبیق داده می شود. این کار تقلب و باگ امتیاز را قابل کشف می کند.

### 5.4 🎖 مدل Badge و UserBadge | نشان ها

تعریف نشان جدا از اهدای آن نگه داری می شود تا اضافه کردن نشان جدید نیاز به تغییر کد نداشته باشد.

مدل Badge یعنی قالب نشان:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه نشان | ارجاع پایدار |
| code | متن یکتا | کد انگلیسی مثل first_step | اتصال منطق اهدا در کد به رکورد بدون وابستگی به شناسه عددی |
| title | متن فارسی | نام نمایشی | نمایش در پروفایل |
| description | متن فارسی | توضیح و راهنمای کسب | انگیزه دادن برای نشان های قفل شده |
| image | فایل تصویری | مدال نشان | نمایش تصویری در دیوار نشان ها |
| rule | انتخابی | شرط اهدا مثل اولین تجربه یا رکورد هفت روزه | اجرای خودکار اهدا توسط موتور گیمیفیکیشن |
| is_active | درست یا غلط | فعال بودن | غیر فعال کردن موقت یک نشان بدون حذف تاریخچه اهداهای قبلی |

مدل UserBadge یعنی اهدا به کاربر:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه اهدا | رهگیری |
| user | ارجاع به کاربر | گیرنده | اتصال به دانش آموز |
| badge | ارجاع به نشان | کدام نشان | اتصال به قالب |
| awarded_at | زمان | زمان اهدا | نمایش تاریخ کسب و مرتب سازی |

یکتایی مرکب روی کاربر و نشان: هر نشان فقط یک بار به هر کاربر داده می شود.

### 5.5 🔥 مدل DailyStreak | رکورد روزهای متوالی

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه رکورد | رهگیری |
| user | ارجاع یکتا به کاربر | صاحب رکورد | هر کاربر فقط یک رکورد جاری دارد |
| current_days | عدد | طول رکورد جاری | نمایش عدد رکورد به کاربر |
| longest_days | عدد | بهترین رکورد تاریخ | نمایش افتخار حتی بعد از شکستن رکورد |
| last_active_date | تاریخ | آخرین روز فعالیت | تشخیص ادامه یا شکست رکورد در روز بعد |
| updated_at | زمان | آخرین به روز رسانی | عیب یابی محاسبه |

دلیل طراحی: تاریخ ها بر اساس منطقه زمانی تهران ذخیره و مقایسه می شوند چون روز کاربر ایرانی با روز جهانی فرق دارد.

### 5.6 📜 مدل Surah و Verse | مرجع قرآن

مدل Surah یعنی شناسنامه سوره:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه داخلی | ارجاع پایدار |
| number | عدد یکتا | شماره سوره از یک تا صد و چهارده | ترتیب استاندارد قرآن و ناوبری |
| title_fa | متن فارسی | نام فارسی سوره | نمایش به کاربر |
| title_ar | متن عربی | نام عربی سوره | اصالت و جستجوی عربی |
| verses_count | عدد | تعداد آیه ها | نمایش اطلاعات و اعتبارسنجی کامل بودن داده |
| revelation | انتخابی | مکی یا مدنی | اطلاعات آموزشی تکمیلی |

مدل Verse یعنی آیه:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه داخلی | ارجاع تجربه ها به آیه |
| surah | ارجاع به سوره | سوره مالک آیه | ساختار درختی و ناوبری |
| number | عدد | شماره آیه در سوره | آدرس استاندارد آیه |
| text_ar | متن عربی | متن اصلی آیه | قلب محتوای قرآنی |
| translation_fa | متن فارسی | ترجمه روان | فهم کاربر فارسی زبان |
| audio | فایل صوتی اختیاری | قرائت آیه | تجربه شنیداری و دسترس پذیری |
| status | انتخابی | وضعیت انتشار | کنترل نمایش در کنار چرخه محتوا |

یکتایی مرکب روی سوره و شماره آیه: هر آیه فقط یک بار ثبت می شود.

### 5.7 🏷 مدل Topic و Concept | موضوع و مفهوم

مدل Topic یعنی دسته بندی موضوعی:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع |
| title_fa | متن فارسی | نام موضوع مثل دوستی | نمایش و فیلتر |
| slug | متن یکتای انگلیسی | نامک اینترنتی | آدرس تمیز و پایدار برای صفحه موضوع |
| description_fa | متن فارسی | توضیح کوتاه | راهنمایی کاربر در صفحه موضوع |
| image | فایل تصویری | تصویر موضوع | کارت های جذاب در خانه و کشف |
| order | عدد | ترتیب نمایش | کنترل چیدمان دستی توسط تحریریه |
| status | انتخابی | وضعیت انتشار | مخفی کردن موقت بدون حذف |

مدل Concept یعنی مفهوم کلیدی که پل بین آیه و تجربه است:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع |
| title_fa | متن فارسی | نام مفهوم در یک عبارت | نمایش در فیلتر و صفحه |
| summary_fa | متن فارسی | توضیح یک یا دو جمله ای | فهم سریع بدون خواندن متن طولانی |
| topics | چند ارجاع به موضوع | موضوعات مرتبط | فیلتر متقابل و پیشنهاد |
| verses | چند ارجاع به آیه | آیه های پشتیبان | اثبات قرآنی مفهوم |
| status | انتخابی | وضعیت انتشار | کنترل نمایش |

### 5.8 ✨ مدل Experience و ExperienceStep | تجربه و گام ها

مدل Experience یعنی قلب محصول:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع مسیرها و کمپین ها و پیشرفت |
| title_fa | متن فارسی | عنوان جذاب تجربه | اولین چیزی که کاربر می بیند و کلیک می کند |
| goal_fa | متن فارسی | هدف یادگیری به زبان ساده | شفافیت انتظار و تعهد محتوایی |
| level | انتخابی | سطح مقدماتی یا متوسط یا پیشرفته | فیلتر و پیشنهاد متناسب با کاربر |
| type | انتخابی | نوع داستانی یا سناریویی یا مفهومی | فیلتر و تنوع تجربه |
| topics | چند ارجاع | موضوعات مرتبط | کشف و پیشنهاد |
| concepts | چند ارجاع | مفاهیم کلیدی | اتصال معنایی و جستجو |
| verses | چند ارجاع | آیه های مبنا | اتصال دو طرفه با قرآن |
| cover | فایل تصویری | تصویر شاخص | کارت جذاب در فهرست ها |
| estimated_minutes | عدد | زمان تقریبی مطالعه | مدیریت انتظار کاربر و فیلتر زمانی |
| points | عدد | امتیاز تکمیل تجربه | انگیزه و شفافیت پاداش قبل از شروع |
| order_hint | متن اختیاری | راهنمای ترتیب پیشنهادی | کمک به سازنده مسیر |
| status | انتخابی | وضعیت چرخه انتشار | جدایی پیش نویس از محتوای عمومی |
| published_at | زمان یا خالی | زمان انتشار | مرتب سازی جدیدترین ها و گزارش |
| created_by | ارجاع به کاربر | سازنده | مسئولیت و گزارش عملکرد تیم |
| approved_by | ارجاع به کاربر یا خالی | تایید کننده نهایی | شفافیت تایید محتوای دینی |

مدل ExperienceStep یعنی هر کدام از نه گام:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع |
| experience | ارجاع به تجربه | تجربه مالک | مالکیت و حذف آبشاری |
| order | عدد | ترتیب گام از یک تا نه | نمایش مرحله ای و نوار پیشرفت |
| kind | انتخابی | نوع گام از معرفی تا تامل | رندر متفاوت هر گام در فرانت |
| title_fa | متن فارسی | تیتر گام | راهنمایی کاربر در هر مرحله |
| body_fa | متن بلند | محتوای گام | متن اصلی یادگیری |
| media | فایل اختیاری | تصویر یا صوت گام | غنای تجربه هر مرحله |

یکتایی مرکب روی تجربه و ترتیب: هر شماره گام فقط یک بار در هر تجربه وجود دارد.

### 5.9 🧩 مدل Activity و ActivityOption | فعالیت و گزینه ها

مدل Activity یعنی تمرین تعاملی:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع و ثبت پاسخ |
| experience | ارجاع به تجربه | تجربه مالک | فعالیت همیشه داخل یک تجربه معنا دارد |
| type | انتخابی | نوع از هفت نوع پشتیبانی شده | انتخاب کامپوننت رندر در فرانت و منطق نمره دهی در بکند |
| question_fa | متن فارسی | صورت تمرین | چیزی که کاربر باید حل کند |
| explanation_fa | متن فارسی | توضیح آموزشی پاسخ | یادگیری بعد از پاسخ که از خود پاسخ مهم تر است |
| points | عدد | امتیاز پاسخ صحیح | پاداش شفاف |
| order | عدد | ترتیب در تجربه | نمایش درست در تجربه های چند فعالیتی |
| status | انتخابی | وضعیت انتشار | کنترل نمایش |

مدل ActivityOption یعنی گزینه هر فعالیت:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ثبت پاسخ کاربر با شناسه نه متن |
| activity | ارجاع به فعالیت | فعالیت مالک | مالکیت |
| text_fa | متن فارسی | متن گزینه | نمایش به کاربر |
| is_correct | درست یا غلط | درست بودن گزینه | نمره دهی در سرور بدون اعتماد به فرانت |
| feedback_fa | متن اختیاری | بازخورد مخصوص این گزینه | توضیح اینکه چرا این گزینه درست یا غلط است |
| order | عدد | ترتیب نمایش | چیدمان دستی گزینه ها |

برای فعالیت وصل کردنی و مرتب سازی، گزینه ها با منطق همان نوع تفسیر می شوند ولی ساختار جدول یکی می ماند تا کد ساده بماند.

### 5.10 📝 مدل Quiz و QuizQuestion | آزمون و سوالات

مدل Quiz یعنی آزمون کوتاه:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع مسیر پایانی و تجربه |
| title_fa | متن فارسی | عنوان آزمون | نمایش |
| description_fa | متن فارسی | توضیح کوتاه | مدیریت انتظار تعداد سوال و زمان |
| pass_score | عدد | حد نصاب قبولی | تشخیص قبولی برای اهدای نشان مسیر |
| time_limit_seconds | عدد یا خالی | سقف زمان | ایجاد چالش زمانی در صورت نیاز |
| status | انتخابی | وضعیت انتشار | کنترل نمایش |

مدل QuizQuestion یعنی سوال آزمون با گزینه های خودش:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ثبت پاسخ |
| quiz | ارجاع به آزمون | آزمون مالک | مالکیت |
| text_fa | متن فارسی | صورت سوال | نمایش |
| options | ساختار JSON | گزینه ها با مشخص بودن صحیح | ساده نگه داشتن مدل چون گزینه آزمون نیازی به بازخورد جدا ندارد |
| correct_index | عدد | اندیس پاسخ صحیح | نمره دهی سروری |
| explanation_fa | متن فارسی | توضیح پاسخ | یادگیری بعد از آزمون |
| score | عدد | امتیاز این سوال | جمع امتیاز آزمون |
| order | عدد | ترتیب | نمایش ثابت و منصفانه |

### 5.11 🛤 مدل LearningPath و PathItem | مسیر و اجزا

مدل LearningPath یعنی برنامه چند مرحله ای:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع کمپین و پیشرفت |
| title_fa | متن فارسی | عنوان مسیر | جذب کاربر |
| goal_fa | متن فارسی | هدف مسیر | تعهد محتوایی و انتخاب آگاهانه |
| cover | فایل تصویری | تصویر شاخص | کارت جذاب |
| estimated_minutes | عدد | زمان کل تقریبی | مدیریت انتظار |
| final_quiz | ارجاع اختیاری به آزمون | آزمون پایانی | سنجش جمع بندی قبل از اهدای نشان |
| badge | ارجاع اختیاری به نشان | نشان پایان مسیر | پاداش هویتی اتمام |
| points | عدد | امتیاز اتمام مسیر | انگیزه تکمیل کل مسیر |
| status | انتخابی | وضعیت انتشار | کنترل نمایش |
| created_by | ارجاع به کاربر | سازنده | مسئولیت |

مدل PathItem یعنی هر قدم مسیر:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| path | ارجاع به مسیر | مسیر مالک | مالکیت |
| experience | ارجاع به تجربه | تجربه این قدم | اتصال به محتوا بدون کپی |
| order | عدد | ترتیب قدم | اجرای هدفمند برنامه آموزشی |

یکتایی مرکب روی مسیر و تجربه: هر تجربه فقط یک بار در هر مسیر می آید.

### 5.12 📣 مدل Campaign | کمپین

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | ارجاع |
| title_fa | متن فارسی | عنوان کمپین | نمایش بنر و صفحه |
| message_fa | متن فارسی | پیام اصلی | قانع سازی شرکت |
| banner | فایل تصویری | بنر | جلب توجه در خانه |
| experiences | چند ارجاع | تجربه های منتخب | برنامه محتوایی بدون کپی |
| paths | چند ارجاع | مسیرهای منتخب | برنامه عمیق تر |
| starts_at | زمان | شروع بازه | فعال سازی خودکار بنر |
| ends_at | زمان | پایان بازه | غیر فعال سازی خودکار |
| is_pinned | درست یا غلط | سنجاق در خانه | اولویت نمایشی کمپین مهم |
| status | انتخابی | وضعیت انتشار | کنترل نمایش جدا از بازه زمانی |

دلیل دو لایه کنترل: status تصمیم تحریریه است و بازه زمانی تصمیم تقویمی. بنر فقط وقتی نمایش داده می شود که هر دو اجازه بدهند.

### 5.13 📊 مدل های پیشرفت | ExperienceProgress و PathProgress و Attempt

مدل ExperienceProgress یعنی وضعیت یک کاربر در یک تجربه:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| user | ارجاع به کاربر | یادگیرنده | مالکیت پیشرفت |
| experience | ارجاع به تجربه | کدام تجربه | اتصال |
| started_at | زمان | شروع | محاسبه مدت یادگیری و شروع رکورد روزانه |
| completed_at | زمان یا خالی | تکمیل | تشخیص اتمام و اهدای امتیاز |
| reflection_text | متن یا خالی | تامل شخصی | ذخیره برداشت کاربر به صورت خصوصی |
| points_earned | عدد | امتیاز کسب شده از این تجربه | شفافیت و جمع بندی |

یکتایی مرکب روی کاربر و تجربه: هر کاربر در هر تجربه فقط یک رکورد پیشرفت دارد و رکورد تکراری ساخته نمی شود.

مدل ActivityAttempt یعنی هر بار پاسخ به فعالیت:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| user | ارجاع به کاربر | پاسخ دهنده | مالکیت |
| activity | ارجاع به فعالیت | کدام تمرین | اتصال |
| selected_ids | ساختار JSON | گزینه های انتخاب شده | بازبینی پاسخ و تحلیل خطاهای رایج |
| is_correct | درست یا غلط | نتیجه | نمره دهی و آمار |
| score_earned | عدد | امتیاز این تلاش | حسابداری دقیق کیف امتیاز |
| duration_seconds | عدد | مدت پاسخ | تحلیل سختی سوال |
| created_at | زمان | زمان ثبت | ترتیب تلاش ها و تشخیص تلاش اول |

مدل QuizAttempt یعنی هر بار شرکت در آزمون:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| user | ارجاع به کاربر | شرکت کننده | مالکیت |
| quiz | ارجاع به آزمون | کدام آزمون | اتصال |
| answers | ساختار JSON | پاسخ همه سوال ها | بازبینی و نمره دهی |
| score | عدد | نمره کسب شده | تشخیص قبولی |
| passed | درست یا غلط | قبولی | شرط اهدای نشان مسیر |
| created_at | زمان | زمان ثبت | تاریخچه تلاش ها |

مدل PathProgress یعنی وضعیت کاربر در مسیر:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| user | ارجاع به کاربر | یادگیرنده | مالکیت |
| path | ارجاع به مسیر | کدام مسیر | اتصال |
| completed_count | عدد | مراحل تکمیل شده | محاسبه درصد پیشرفت |
| total_count | عدد | کل مراحل در زمان شروع | ثابت ماندن درصد حتی اگر بعدا مرحله اضافه شود |
| completed_at | زمان یا خالی | اتمام مسیر | اهدای نشان و امتیاز پایانی |

دلیل ذخیره total_count: اگر تحریریه بعدا به مسیر مرحله اضافه کند، درصد کاربران قبلی به هم نمی ریزد.

### 5.14 🏫 مدل School | رکورد مدرسه همکار

تاکید: این جدول فقط دفتر تلفن هوشمند تیم مدیریت است. هیچ ورود، هیچ پنل و هیچ دسترسی برای مدرسه وجود ندارد.

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| name | متن فارسی | نام مدرسه | شناسایی |
| city | متن فارسی | شهر | گزارش منطقه ای و برنامه بازاریابی |
| contact_name | متن فارسی | نام مسئول ارتباط | دانستن اینکه با چه کسی حرف می زنیم |
| contact_phone | شماره | تلفن مسئول | پیگیری همکاری |
| status | انتخابی | وضعیت همکاری: فعال یا غیر فعال یا در مذاکره | مدیریت قیف همکاری مدرسه ها |
| notes | متن بلند | یادداشت پیگیری | ثبت تاریخچه مذاکره و تعهدات |
| created_by | ارجاع به کاربر | ثبت کننده | مسئولیت رکورد |

### 5.15 🔔 مدل Notification و 📋 مدل AuditLog

مدل Notification یعنی پیام به کاربر:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| user | ارجاع به کاربر | گیرنده | ارسال شخصی |
| title_fa | متن فارسی | تیتر پیام | نمایش در فهرست اعلان ها |
| body_fa | متن فارسی | متن پیام | توضیح |
| link | متن یا خالی | آدرس مقصد | هدایت به صفحه مرتبط مثل تجربه جدید |
| is_read | درست یا غلط | خوانده شده یا نه | نشان پیام خوانده نشده |
| created_at | زمان | زمان ساخت | مرتب سازی |

مدل AuditLog یعنی دفتر ثبت تغییرات مهم:

| فیلد | نوع | معنی | چرا لازم است |
| --- | --- | --- | --- |
| id | عدد یکتا | شناسه | رهگیری |
| actor | ارجاع به کاربر | انجام دهنده | دانستن اینکه چه کسی چه کرد |
| action | انتخابی | نوع عمل مثل انتشار یا مسدود سازی | دسته بندی و جستجو |
| target_type | متن | نوع رکورد هدف | فهم موضوع لاگ |
| target_id | متن | شناسه رکورد هدف | ارجاع دقیق |
| detail | ساختار JSON | جزئیات تغییر | بازسازی اتفاق در بررسی اختلاف |
| created_at | زمان | زمان ثبت | ترتیب تاریخی |

دلیل وجود AuditLog: محتوای دینی و دسترسی کاربران حساس است و هر انتشار یا مسدود سازی باید قابل حسابرسی باشد.

### نمودار ارتباط کلی

```text
User
├── VerificationCode
├── PointTransaction
├── UserBadge
├── DailyStreak
├── ExperienceProgress
├── ActivityAttempt
├── QuizAttempt
├── PathProgress
└── Notification

Surah
└── Verse
    └── Concept
        └── Experience
            ├── ExperienceStep
            ├── Activity
            │   └── ActivityOption
            └── Quiz
                └── QuizQuestion

LearningPath
└── PathItem
    └── Experience

Campaign
├── Experience
└── LearningPath

School (مستقل و بدون ارتباط به کاربر)
AuditLog (مستقل و فقط خواندنی)
```

---

## 6. 🎮 گیمیفیکیشن | طراحی فنی کامل

### جدول کسب امتیاز

| رویداد | امتیاز پیشنهادی | سقف | دلیل سقف |
| --- | --- | --- | --- |
| تکمیل تجربه | ده | یک بار برای هر تجربه | جلوگیری از فارم کردن با تکرار |
| پاسخ صحیح فعالیت در تلاش اول | پنج | هر فعالیت یک بار کامل | تشویق دقت نه حدس |
| پاسخ صحیح در تلاش بعدی | دو | همان سقف | یادگیری هنوز پاداش دارد ولی کمتر |
| قبولی آزمون | پانزده | هر آزمون یک بار کامل | ارزش جمع بندی بیشتر است |
| اتمام مسیر | سی | هر مسیر یک بار | پاداش بزرگ برای تعهد بزرگ |
| ثبت تامل شخصی | سه | روزی حداکثر سه بار | تشویق تفکر بدون اسپم |
| روز فعال برای رکورد | دو | روزی یک بار | پاداش عادت روزانه |

منظور از فارم کردن یعنی سوء استفاده با تکرار مکانیکی یک عمل برای جمع امتیاز. سقف ها دقیقا برای همین است.

### قوانین ضد تقلب امتیاز

| کد | قانون | پیاده سازی |
| --- | --- | --- |
| BR-GAME-1 | فرانت هیچ وقت مبلغ امتیاز را نمی فرستد | بکند از روی نوع رویداد خودش حساب می کند |
| BR-GAME-2 | هر رویداد با reference_id یکتا ثبت می شود | ثبت دوباره همان عمل نادیده گرفته می شود |
| BR-GAME-3 | سقف روزانه امتیاز از فعالیت تکراری اعمال می شود | شمارش تراکنش های امروز قبل از ثبت جدید |
| BR-GAME-4 | الگوی مشکوک مثل صد پاسخ در یک دقیقه پرچم می خورد | بررسی سرعت در ثبت و ثبت در لاگ |
| BR-GAME-5 | امتیاز کاربر مسدود شده در رتبه بندی نمی آید | فیلتر وضعیت حساب در کوئری رتبه بندی |

### سطوح کاربر

| سطح | مجموع امتیاز لازم | دلیل |
| --- | --- | --- |
| تازه وارد | صفر | نقطه شروع |
| جوینده | صد | حدود ده تجربه یعنی دو هفته فعالیت |
| پوینده | سیصد | حدود یک ماه فعالیت منظم |
| راهنما | هفتصد | کاربر وفادار چند ماهه |
| ستاره پتار | هزار و پانصد | چهره شاخص جامعه |

سطح فقط نمایشی و انگیزشی است و هیچ دسترسی اضافه نمی دهد تا امنیت ساده بماند.

### نشان های اولیه

| کد نشان | نام | شرط اهدا |
| --- | --- | --- |
| first_step | اولین قدم | تکمیل اولین تجربه |
| truthful | راستگو | ده پاسخ صحیح پیاپی |
| week_warrior | هفت روزه | رکورد هفت روز متوالی |
| path_opener | مسیرگشا | اتمام اولین مسیر |
| ramadan_star | ستاره رمضان | تکمیل پنج تجربه کمپین رمضان |
| thinker | اندیشمند | ثبت بیست تامل شخصی |
| perfect_quiz | بی نقص | نمره کامل در یک آزمون |

اضافه کردن نشان جدید فقط ساخت یک رکورد Badge با rule جدید است و نیازی به تغییر ساختار ندارد.

### الگوریتم رکورد روزانه

1. هر فعالیت یادگیری واقعی یعنی تکمیل تجربه یا فعالیت یا آزمون، روز جاری کاربر را فعال می کند.
2. روز بر اساس منطقه زمانی تهران حساب می شود.
3. اگر امروز فعال شود و دیروز هم فعال بوده، رکورد یک واحد زیاد می شود.
4. اگر دیروز فعال نبوده و امروز فعال شود، رکورد از یک شروع می شود.
5. محاسبه در لحظه ثبت رویداد انجام می شود و یک کار شبانه صحت رکوردها را بازبینی می کند.

### رتبه بندی

| جدول | بازه امتیاز | به روز رسانی |
| --- | --- | --- |
| هفتگی | شنبه تا جمعه همین هفته | هر پانزده دقیقه با کار پس زمینه |
| کلی | همه زمان ها | هر یک ساعت با کار پس زمینه |
| ویژه کمپین | بازه کمپین | هر پانزده دقیقه در دوره کمپین |

دلیل محاسبه دوره ای: رتبه بندی زنده روی هر درخواست برای هزاران کاربر سنگین است. محاسبه دوره ای و کش در Redis کافی و سریع است.

### API های گیمیفیکیشن

| روش | مسیر | هدف |
| --- | --- | --- |
| GET | /api/v1/me/points | مجموع امتیاز و تاریخچه تراکنش ها |
| GET | /api/v1/me/level | سطح فعلی و فاصله تا سطح بعد |
| GET | /api/v1/me/badges | نشان های من |
| GET | /api/v1/me/streak | وضعیت رکورد روزانه |
| GET | /api/v1/leaderboard/weekly | جدول هفتگی |
| GET | /api/v1/leaderboard/all-time | جدول کلی |
| GET | /api/v1/leaderboard/me | جایگاه من |

### وظایف فرانت در گیمیفیکیشن

نمایش امتیاز، نوار پیشرفت سطح، دیوار نشان ها، عدد رکورد با انیمیشن، جدول رتبه بندی و پیام تبریک بعد از هر کسب. فرانت هیچ محاسبه ای انجام نمی دهد و فقط نمایش دهنده پاسخ بکند است.

---

## 7. ⚙ فرایندهای پس زمینه

کار پس زمینه یعنی کاری که بعد از درخواست کاربر در صف اجرا می شود تا پاسخ سریع برگردد. مجری آن Celery و زمان بند آن Celery Beat است.

| کار | زمان اجرا | چه می کند | چرا پس زمینه است |
| --- | --- | --- | --- |
| expire_codes | هر ده دقیقه | کدهای منقضی را باطل و قدیمی ها را پاک می کند | جدول کدها باد نکند |
| rebuild_weekly_leaderboard | هر پانزده دقیقه | جدول هفتگی را می سازد و در Redis می گذارد | محاسبه سنگین نباید روی درخواست کاربر باشد |
| rebuild_alltime_leaderboard | هر یک ساعت | جدول کلی را می سازد و کش می کند | همان دلیل |
| repair_streaks | هر شب ساعت دو بامداد | رکوردهای ناسازگار را اصلاح می کند | اطمینان از صحت بعد از خطاهای احتمالی |
| award_pending_badges | هر پنج دقیقه | شرایط نشان های معلق را بررسی و اهدا می کند | اهدا ممکن است به چند رویداد وابسته باشد |
| send_notifications | پیوسته از صف | پیامک و ایمیل و اعلان را می فرستد | ارسال پیام کند است و نباید کاربر را معطل کند |
| cleanup_audit | هفتگی | لاگ های قدیمی تر از حد سیاست را آرشیو می کند | کنترل حجم دیتابیس |
| refresh_suggestions | هر شب | پیشنهاد بعدی هر کاربر فعال را از قبل حساب می کند | صفحه یادگیری من سریع باز شود |
| campaign_switch | هر پنج دقیقه | کمپین هایی که بازه شان شروع یا تمام شده را جابه جا می کند | بنر همیشه با تقویم هماهنگ باشد |
| media_optimize | با هر آپلود | تصویر را فشرده و بند انگشتی می سازد | سرعت بارگذاری صفحه ها |

سیاست تلاش مجدد: هر کار ناموفق با فاصله نمایی سه بار تلاش می شود و بعد در صف مرده برای بررسی دستی می رود.

---

## 8. ⚡ راهبرد کش

کش یعنی نگه داری کپی سریع داده پرتکرار تا دیتابیس اذیت نشود. برای تازه کارها: هر داده ای که زیاد خوانده و کم نوشته می شود کاندیدای کش است.

### لایه های کش

| لایه | چه چیزی | ابزار | انقضا |
| --- | --- | --- | --- |
| مرورگر و CDN | تصویر، صوت و فایل ثابت | هدر کش HTTP | طولانی با نسخه بندی فایل |
| محتوای منتشر شده | تجربه، مسیر، کمپین و قرآن | Redis | تا انتشار بعدی همان رکورد |
| جدول های رتبه بندی | خروجی آماده سه جدول | Redis | پانزده دقیقه تا یک ساعت |
| پیشنهادها | پیشنهاد بعدی کاربران فعال | Redis | یک شبانه روز |
| کد تایید و محدودیت نرخ | شمارش ارسال و تلاش | Redis | چند دقیقه |
| نشست ورود | توکن های معتبر و بلاک لیست | Redis | به اندازه عمر توکن |

### هرگز کش نمی شود

پیشرفت شخصی، پاسخ های ثبت شده، کیف امتیاز لحظه ای، صف استودیو و لاگ. دلیل مشترک: شخصی یا حساس به تازگی هستند و کش قدیمی در آن ها خطای واقعی می سازد.

### کلیدها و ابطال

1. کلید هر رکورد محتوایی شامل نوع و شناسه و نسخه انتشار است تا با انتشار جدید کلید عوض شود.
2. با هر انتشار، آرشیو یا ویرایش محتوای منتشر شده، کش همان رکورد و کش فهرست های مرتبط باطل می شود.
3. ابطال با سیگنال بعد از ذخیره در بکند انجام می شود تا هیچ کدی جا نماند.
4. صفحه خانه یک کلید تجمیعی دارد که با هر انتشار محتوا باطل می شود.

### کش فرانت

فرانت از کش معنایی Next.js برای صفحه های عمومی استفاده می کند و با انتشار جدید از طریق بازاعتبارسنجی تازه می شود. داده شخصی هرگز در کش مشترک فرانت نمی رود.

---

## 9. 📡 قرارداد سراسری API

### اصول پایه

| موضوع | قرارداد |
| --- | --- |
| آدرس پایه | نسخه بندی شده مثل ‎/api/v1/‎ و نسخه بعدی بدون شکستن قبلی اضافه می شود |
| قالب | همیشه JSON با کد وضعیت درست HTTP |
| احراز هویت | هدر Authorization با توکن دسترسی |
| زبان خطا | پیام فارسی کاربرپسند به همراه کد انگلیسی برای کد |
| صفحه بندی | یکدست برای همه فهرست ها با پارامترهای page و page_size |
| مرتب سازی | پارامتر ordering با علامت منفی برای نزولی |
| فیلتر | پارامتر هم نام فیلد برای فیلتر دقیق |

### envelope صفحه بندی

```json
{
  "count": 120,
  "next": "/api/v1/experiences/?page=3",
  "previous": "/api/v1/experiences/?page=1",
  "results": []
}
```

### envelope خطا

```json
{
  "code": "invalid_code",
  "message": "کد وارد شده درست نیست",
  "details": {}
}
```

### محدودیت نرخ پیش فرض

| گروه | سقف |
| --- | --- |
| ارسال کد تایید | پنج بار در ساعت برای هر مقصد |
| ورود با رمز | ده بار در ده دقیقه برای هر آی پی |
| ثبت پاسخ فعالیت | شصت بار در دقیقه برای هر کاربر |
| API عمومی خواندنی | سیصد درخواست در دقیقه برای هر آی پی |

### یکتایی ثبت ها

ثبت پاسخ فعالیت و آزمون و تکمیل تجربه باید در برابر ارسال دوباره مقاوم باشد. فرانت برای هر اقدام یک شناسه یکتا می سازد و می فرستد و بکند تکراری را همان پاسخ قبلی بر می گرداند. این کار اثر دکمه دوباره و اینترنت ناپایدار را خنثی می کند.

---

## 10. 🗂 ساختار مخازن

### فرانت اند Next.js

```text
app
├── page.tsx
├── discover
├── experiences/[id]
├── quran
├── paths/[id]
├── campaigns/[id]
├── leaderboard
├── my-learning
├── profile
├── studio
├── admin-panel
├── login
├── register
├── layout.tsx
└── error.tsx

components
├── ui
├── experience
├── activity
├── quiz
├── path
├── gamification
└── studio

lib
├── api-client
├── auth
└── utils

types
└── api.ts
```

### بکند Django

```text
accounts
├── models User و VerificationCode
├── auth با توکن و کد
└── مدیریت کاربران

content
├── models سوره و آیه و موضوع و مفهوم و تجربه و گام
├── فعالیت و آزمون
└── مسیر و کمپین

learning
├── پیشرفت و تلاش ها
├── موتور نمره دهی
└── پیشنهاد بعدی

gamification
├── امتیاز و نشان و رکورد
├── رتبه بندی
└── قوانین ضد تقلب

studio
├── چرخه انتشار
├── صف بازبینی
└── سابقه تغییرات

management
├── کاربران و مدرسه ها
├── گزارش ها
└── لاگ حسابرسی

notifications
└── صف ارسال پیام

analytics
├── ثبت رویداد
└── گزارش ساز
```

دلیل جدایی اپ ها: هر تیم روی یک دامنه کار می کند و تغییر امتیاز نباید به کد قرآن دست بزند.

---

## 11. 🎨 الزام های طراحی و دسترس پذیری

خلاصه اجرایی سند DESIGN.md برای توسعه دهنده:

1. فقط حالت روشن با پس زمینه سفید غالب.
2. رنگ اصلی آبی آسمانی و کمکی سبز و زرد کم مصرف.
3. حاشیه خاکستری استاندارد بدون سایه و بدون گرادیان.
4. راست به چپ کامل برای فارسی و چپ به راست برای انگلیسی با ویژگی های منطقی CSS.
5. همه عناصر تعاملی نشانگر کلیک و گذار هفتصد میلی ثانیه ای دارند.
6. هویت اسلامی فقط با هندسه ظریف و مدرن در بخش های معرفی و حالت های خالی.
7. سطح دسترس پذیری AA شامل صفحه کلید، فوکوس مشخص، برچسب دسترس پذیر، کنتراست کافی و احترام به کاهش حرکت.
8. فونت فارسی استعداد برای فارسی و لاتین.

---

## 12. 📊 رویدادهای تحلیلی

هر رویداد توسط فرانت یا بکند ثبت و در انبار تحلیل نگه داری می شود. ستون ثبت کننده مشخص می کند مسئول ارسال کیست.

| رویداد | ثبت کننده | معنی |
| --- | --- | --- |
| experience_started | بکند | شروع واقعی یک تجربه |
| experience_completed | بکند | اتمام کامل با فعالیت و تامل |
| activity_submitted | بکند | ثبت هر پاسخ با نتیجه |
| quiz_completed | بکند | ثبت نهایی آزمون با نمره |
| path_started | بکند | شروع مسیر |
| path_completed | بکند | اتمام مسیر و اهدای نشان |
| points_earned | بکند | هر تراکنش امتیاز |
| badge_awarded | بکند | هر اهدای نشان |
| campaign_viewed | فرانت | مشاهده بنر یا صفحه کمپین |
| search_performed | فرانت | اجرای جستجو با فیلترها |
| lead_submitted | بکند | ثبت فرم تماس مدرسه |
| login_failed | بکند | ورود ناموفق برای امنیت |

قانون: تحلیل برای بهبود یادگیری است و بهینه سازی کور روی زمان ماندن ممنوع است.

---

## 13. 🌍 زبان و منطقه زمانی

1. زبان پیش فرض فارسی با جهت راست به چپ است و انگلیسی با چپ به راست پشتیبانی می شود.
2. همه متن نمایشی از فایل ترجمه می آید و متن ثابت در کامپوننت ممنوع است.
3. منطقه زمانی رسمی تهران است و همه محاسبات رکورد و کمپین و رتبه هفتگی با آن انجام می شود.
4. زمان ها در API به صورت استاندارد جهانی با آفست بر می گردد و فرانت به وقت محلی نمایش می دهد.

---

## 14. 🚀 استقرار و مقیاس پذیری

### تصویر تولید

```text
کاربران
↓
توزیع کننده بار
├── فرانت Next.js چند نمونه
└── بکند Django چند نمونه
    ├── دیتابیس اصلی با نسخه فقط خواندنی
    ├── Redis برای کش و صف
    ├── صف Celery با چند کارگر
    └── فضای ذخیره فایل با CDN
```

### اصول مقیاس پذیری

1. نمونه های بکند بدون حالت هستند و هر درخواستی را هر نمونه می تواند جواب بدهد.
2. خواندن های سنگین گزارش از نسخه فقط خواندنی دیتابیس می آید.
3. فایل ها هرگز روی دیسک سرور نگه داری نمی شوند.
4. مهاجرت های دیتابیس بدون توقف سرویس و برگشت پذیر نوشته می شوند.
5. هر انتشار با سلامت سنج خودکار و امکان برگشت سریع انجام می شود.

### چه چیزهایی در شروع لازم نیست

این فهرست برای جلوگیری از مهندسی بیش از حد در شروع است ولی طراحی نباید جلوی اضافه شدن آن ها را بگیرد:

| مورد | وضعیت در شروع | جایگزین شروع |
| --- | --- | --- |
| موتور جستجوی جدا | لازم نیست | جستجوی دیتابیس کافی است |
| خوشه چند نمونه ای | لازم نیست | یک نمونه قوی فرانت و یک نمونه بکند |
| نسخه فقط خواندنی دیتابیس | لازم نیست | همان دیتابیس اصلی |
| CDN جهانی | لازم نیست | هدر کش و یک فضای فایل ساده |
| سامانه لاگ متمرکز | لازم نیست | فایل لاگ ساختار یافته و هشدار پایه |
| کوبرنتیز | لازم نیست | استقرار ساده با کانتینر |
| اپ موبایل مستقل | لازم نیست | وب واکنش گرا |

هر کدام با رشد کاربران بدون تغییر قرارداد API اضافه می شوند.

---

## 15. ✔ چک لیست تحویل تولید

تیم توسعه وقتی کارش تمام است که همه موارد زیر برقرار باشد:

1. ثبت نام هر سه روش و ورود با رمز و کد بدون خطا کار کند.
2. قفل موقت، انقضای کد و ابطال توکن تست شده باشد.
3. کنترل دسترسی هر API با کاربر کم دسترسی تست شده باشد.
4. تجربه کامل نه گام از شروع تا امتیاز کار کند.
5. نمره دهی فقط در سرور انجام شود و دستکاری فرانت بی اثر باشد.
6. امتیاز تکراری با ارسال دوباره ثبت نشود.
7. نشان ها خودکار اهدا شوند و رکورد با منطقه زمانی تهران درست باشد.
8. رتبه بندی دوره ای به روز شود و کاربر مسدود در آن نباشد.
9. انتشار بدون تایید نهایی از هیچ مسیری ممکن نباشد.
10. کش محتوا با انتشار باطل شود و محتوای قدیمی نمایش نماند.
11. بنر کمپین با شروع و پایان بازه خودکار جابه جا شود.
12. چیدمان فارسی و انگلیسی و دسترس پذیری پایه برقرار باشد.
13. خطاهای فارسی و یکدست برای همه حالت ها برگردد.
14. پشتیبان گیری روزانه دیتابیس و تمرین بازیابی انجام شده باشد.
15. ظاهر محصول با سند DESIGN.md مغایرت نداشته باشد.
